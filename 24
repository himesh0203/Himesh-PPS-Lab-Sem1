Write a program in C to read 10 numbers from the keyboard and find their sum and average. Test Data : Input the 10 numbers : Number-1 :2








#include <stdio.h>  
int main() 
{
    int numbers[10];  
    int sum = 0;      
    float average;    
    printf("Input the 10 numbers:\n");
    for (int i = 0; i < 10; i++) 
{
    printf("Number-%d: ", i + 1);  
    scanf("%d", &numbers[i]);      
    sum += numbers[i];          
}
    average = sum / 10.0;  
    printf("The Sum is: %d\n", sum);              
    printf("The Average is: %.2f\n", average);    
    return 0; 
}
